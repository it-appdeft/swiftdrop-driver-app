import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SwiftDrop app smoke test', (WidgetTester tester) async {
    // Full app integration test requires Firebase init — run on device/emulator.
    // Unit tests for individual controllers are added alongside each module.
    expect(true, isTrue);
  });
}
